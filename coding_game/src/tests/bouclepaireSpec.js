const { logEvenNumbers } = require('../bouclepaire.js');

describe('bouclepaire', () => {

it('expect myFuncPalandrome(radar) to equal/tobe radar,', () => {
  expect(logEvenNumbers (10)).toEqual([2,4,6,8]);
});


})